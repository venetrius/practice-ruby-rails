<%= form_with(model: @particle, local: true) do |form| %>
  <%# Show errors if any%>
  <% if @particle.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@particle.errors.count, "error") %> prohibited
        this particle from being saved:
      </h2>
      <ul>
        <% @particle.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<%# Render form %>
  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>

  <p>
    <%= form.label :spin %><br>
    <%= form.text_field :spin %>
  </p>

    <p>
    <%= form.label :color %><br>
    <%= form.text_field :color %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>
